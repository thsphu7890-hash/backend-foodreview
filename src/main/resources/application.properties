spring.application.name=foodreview
server.port=8080

# Ưu tiên lấy từ biến môi trường của Render, nếu không có mới dùng localhost
spring.datasource.url=${MYSQL_URL:jdbc:mysql://localhost:3306/food_review}
spring.datasource.username=${MYSQL_USER:root}
spring.datasource.password=${MYSQL_PASSWORD:}

# Cấu hình Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Quan trọng: Đảm bảo driver được khai báo đúng
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# --- CÁC CẤU HÌNH KHÁC GIỮ NGUYÊN ---
# (JWT, Gemini AI, Cors...)

# ==============================================================
# 2. CẤU HÌNH AI (Giữ nguyên)
# ==============================================================
gemini.api-key=AIzaSyCq7GAEZ7nmOAx9uuLyGRu1X3tk_pCkTkk
gemini.model-name=gemini-1.5-flash-001
spring.ai.retry.max-attempts=1

# ==============================================================
# 3. CẤU HÌNH JWT & WEB (Giữ nguyên)
# ==============================================================
spring.data.web.pageable.serialization-mode=via-dto

jwt.secret-key=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
jwt.expiration=86400000